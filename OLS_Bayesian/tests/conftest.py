import pytest
import sys
import os
import numpy as np
# Add src directory to Python path for imports
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'src'))
# Import the modules to test (is it necessary to import them here?)

